apiVersion: v1
kind: Service
metadata:
  name: {{ include "processingservice.fullname" . }}
  labels:
    {{- include "processingservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "processingservice.selectorLabels" . | nindent 4 }}
